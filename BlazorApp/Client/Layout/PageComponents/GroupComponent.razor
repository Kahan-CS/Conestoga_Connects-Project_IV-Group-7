@using Microsoft.AspNetCore.Components
@using BlazorApp.Client.Layout.PageComponents

<div class="group-item">
    <span>@Name</span>
    <div class="button-container">
        <ButtonComponent Text="Delete" BackgroundColor="#ff4d4d" TextColor="#ffffff" HoverColor="#cc0000" OnClick="@DeleteClicked" />
        <ButtonComponent Text="Edit" BackgroundColor="#25d422" TextColor="#ffffff" HoverColor="#1a9e17" OnClick="@EditClicked" />
        <ButtonComponent Text="View" BackgroundColor="#38bff0" TextColor="#ffffff" HoverColor="#2887a9" OnClick="@ViewClicked" />
    </div>
</div>

@code {
    [Parameter]
    public string Name { get; set; }

    private void DeleteClicked()
    {
        // Implement delete logic
    }

    private void EditClicked()
    {
        // Implement edit logic
    }

    private void ViewClicked()
    {
        // Implement view logic
    }
}

<style>
    .group-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px 0;
        border-bottom: 1px solid #ccc;
    }

        .group-item span {
            flex: 1;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

    .button-container {
        display: flex;
        gap: 10px;
    }
</style>
